[package]
name = "bluesea-identity"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rand = "0.8"
multiaddr = { workspace = true }
parking_lot = { workspace = true }
serde = { workspace = true }