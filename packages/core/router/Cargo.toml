[package]
name = "atm0s-sdn-router"
version = "0.1.0"
edition = "2021"


[features]
mock = ["mockall"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
atm0s-sdn-identity = { workspace = true }
mockall = { workspace = true, optional = true }
